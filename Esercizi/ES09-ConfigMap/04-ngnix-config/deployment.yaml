apiVersion: apps/v1
kind: Deployment
metadata:
  name: nginx-custom
  namespace: prj-antonio-minelli
  labels:
    app: nginx-custom
    scenario: cm-nginx-config
spec:
  replicas: 1
  selector:
    matchLabels:
      app: nginx-custom
  template:
    metadata:
      labels:
        app: nginx-custom
        scenario: cm-nginx-config
    spec:
      containers:
        - name: nginx
          image: nginx:1.25-alpine
          ports:
            - containerPort: 80
          volumeMounts:
            # Mount con subPath: sovrascrive un singolo file, non l'intera directory.
            # ATTENZIONE: con subPath il file NON si aggiorna automaticamente
            # se il ConfigMap cambia (serve riavvio del pod).
            - name: nginx-conf
              mountPath: /etc/nginx/conf.d/default.conf
              subPath: default.conf
            - name: nginx-conf
              mountPath: /usr/share/nginx/html/index.html
              subPath: index.html
            - name: nginx-cache
              mountPath: /var/cache/nginx 
            - name: nginx-run
              mountPath: /var/run
          resources:
            requests:
              cpu: 50m
              memory: 64Mi
            limits:
              cpu: 100m
              memory: 128Mi
      volumes:
        - name: nginx-conf
          configMap:
            name: nginx-custom-config
        - name: nginx-cache
          emptyDir: {}  # volume temporaneo per testare la struttura interna del ConfigMap montato
        - name: nginx-run
          emptyDir: {}  # volume temporaneo per testare la struttura interna del ConfigMap montato
---
apiVersion: v1
kind: Service
metadata:
  name: nginx-custom-svc
  namespace: prj-antonio-minelli
  labels:
    scenario: cm-nginx-config
spec:
  selector:
    app: nginx-custom
  ports:
    - port: 80
      targetPort: 8080
